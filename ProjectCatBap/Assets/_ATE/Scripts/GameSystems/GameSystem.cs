using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace ATE.GameSystems
{
	public abstract class GameSystem : MonoBehaviour
	{
        /*protected static GameSystem baseInstance = null;

        private void Awake()
        {
            // Singleton
            if (baseInstance == null)
                baseInstance = this;
            else if (baseInstance != this)
                Destroy (gameObject);
        }*/
		
	}
}